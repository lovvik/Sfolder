<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- ConstraintLayout contenant les autres vues -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/mainview"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="0dp"
            android:layout_height="49dp"
            android:background="?attr/colorPrimary"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="1.0"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            >

            <!-- Boutons dans la barre d'outils -->
            <ImageButton
                android:id="@+id/btnOpenFolder"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@color/cardview_shadow_end_color"
                android:contentDescription="Ouvrir le dossier"
                android:src="@drawable/icons8_plus_30"
                tools:layout_editor_absoluteX="16dp"
                tools:layout_editor_absoluteY="12dp"
                android:layout_margin="10dp"/>
            <ImageButton
                android:id="@+id/btnResetPassword"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@color/cardview_shadow_end_color"
                android:contentDescription="Ouvrir le dossier"
                android:src="@drawable/icons8_forgot_password_30"
                tools:layout_editor_absoluteX="16dp"
                tools:layout_editor_absoluteY="12dp"
                android:layout_margin="10dp"/>


        </androidx.appcompat.widget.Toolbar>

        <ListView
            android:id="@+id/listView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:clickable="true"
            android:longClickable="false"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toBottomOf="@id/toolbar" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- NavigationView pour le tiroir -->

</androidx.drawerlayout.widget.DrawerLayout>
